# Example environment variables for development
# Copy this file to .env and replace the placeholder values with your own secrets

PORT=5000
# MongoDB connection string. Replace <username>, <password>, and <cluster-url> with your Atlas credentials or use a local URI.
# Example Atlas: mongodb+srv://<username>:<password>@cluster0.mongodb.net/solarDB
# Example local: mongodb://localhost:27017/solarDB
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/solarDB
# Example Atlas: mongodb+srv://<username>:<password>@cluster0.mongodb.net/solarDB
# Example local: mongodb://localhost:27017/solarDB
# NOTE: Never commit real credentials to the repo — keep them only in your local `.env` file.

# JWT secret for signing tokens
JWT_SECRET=your_jwt_secret_here

# Optional
# NODE_ENV=development
# DEBUG=your_app:*
